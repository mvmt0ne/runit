rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 단일 사용자: 본인 uid 하위 문서만 읽기/쓰기 허용
    match /users/{uid}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
